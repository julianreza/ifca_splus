import {ProfileRoutes} from './ProfileRoutes';

export const ProfileConfig = {
    settings: {
        layout: {
            config: {}
        }
    },
    routes  : [
        ...ProfileRoutes
    ]
};
